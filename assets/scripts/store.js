'use strict'

const store = {
}

const stackMode = 'gre'

const greFlashcardId = null

const arrayOfMyFlashcards = []

const currentFlashcardId = null

const flashcard = {}

module.exports = {
  store,
  greFlashcardId,
  arrayOfMyFlashcards,
  stackMode,
  currentFlashcardId,
  flashcard
}
